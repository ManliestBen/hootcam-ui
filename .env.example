# Hootcam backend API base URL (used for all API requests and for public camera streams when not logged in).
# - 3-part setup: use your NUC running Hootcam Motion (e.g. http://192.168.1.5:8080)
# - All-in-one setup: use your Pi running Hootcam Server (e.g. http://192.168.1.10:8080)
# Defaults to http://localhost:8080 if unset.
# VITE_HOOTCAM_STREAMER_URL=http://localhost:8080
