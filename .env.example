# Hootcam server API base URL (used for all API requests and for public camera streams when not logged in).
# Defaults to http://localhost:8080 if unset.
# VITE_HOOTCAM_SERVER_URL=http://localhost:8080
